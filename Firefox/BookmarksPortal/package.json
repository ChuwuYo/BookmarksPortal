{
    "scripts": {
      "build:firefox": "web-ext build --source-dir dist --artifacts-dir web-ext-artifacts --overwrite-dest",
      "build:firefox-prod": "web-ext build --source-dir dist --artifacts-dir web-ext-artifacts --overwrite-dest --filename bookmarks-portal-firefox.xpi"
    }
  }
  